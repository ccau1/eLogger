'use strict';

angular.module(Constants.Module).controller('VehicleSettingsController', ['$scope', '$log', '$reactive', '$state', function ($scope, $log, $reactive, $state) {
    $scope.$on('$viewContentLoaded', function () {

    });

    var reactiveContext = $reactive(this).attach($scope);
    var me = this;



}]);